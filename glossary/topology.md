---
layout: entry
title: topology
aliases:
type:
  - definition
reference:
course:
lecture:
statement:
created: 2025-04-13T19:50:24-04:00
tags:
  - topology
---

## Definition

Given [[set]] $X$, a **topology** on $X$ is a collection $\tau$ of _subsets_ of $X$ called _open subsets_ ([[open set|open sets]]), where $\tau \subset P(X)$ (subset of _power set_) such that it is closed under forming

1. finite intersections (any intersection of finitely many elements of $\tau$ is an element of $\tau$)
2. arbitrary unions (any union of of elements of $\tau$ is a member of $\tau$)

where a [[topological space]] $(X,\tau)$ is a set $X$ equipped with such a _topology_.

It may be shown that the _empty set_ $\emptyset$ and $X$ are elements of $\tau$, i.e. $\emptyset \in \tau$, $X \in \tau$.

---

## References

1. https://ncatlab.org/nlab/show/topology
2. https://en.wikipedia.org/wiki/Topology
3. https://ncatlab.org/nlab/show/Introduction+to+Topology
